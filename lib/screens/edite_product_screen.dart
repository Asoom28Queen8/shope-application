import 'package:flutter/material.dart';

class Edite_Product_Screen extends StatelessWidget {
  //const Edite_Product_Screen({ Key? key }) : super(key: key);
  static const routeName = '/Edite_product_screen';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Edite Product Screen'),
      ),
      body: Center(
        child: Text('Edite Product Screen'),
      ),
    );
  }
}
